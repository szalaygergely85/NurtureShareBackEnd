# Gradle Wrapper

Run `gradle wrapper --gradle-version 8.7` to generate the wrapper files (gradlew, gradlew.bat, gradle/wrapper/gradle-wrapper.jar, gradle/wrapper/gradle-wrapper.properties).

This command requires Gradle 8.7 to be installed locally, or you can download it from https://gradle.org/releases/.
